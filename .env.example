PORT=8787

# Any random string. Share it only with your faction so random people can't
# hit your server. Leave blank to disable auth (fine for local testing).
SERVER_SECRET=change-me-to-a-random-string
